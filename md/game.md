# 【確定版】「積みゲー管理」メイン画面（ライブラリ） UI構成案

## 1. 全体テーマ・配色
* **ベースカラー:** ログイン画面と統一したダークネイビー（`#0f172a`）。
* **アクセントカラー:** シアン（`#00e5ff`）と紫（`#7c3aed`）のネオン発光。
* **マテリアル:** すりガラス効果（`backdrop-filter: blur`）を多用し、奥行きを出す。
* **アイコンライブラリ:** **Lucide Icons**（シャープでモダンな線画アイコン）。

---

## 2. トップエリア：ゲーミングHUD（統計ダッシュボード）
巨大な3つのカードを廃止し、スリムでスタイリッシュな「1つの横長パネル（Bento Grid風）」に統合。

* **背景:** すりガラス（`rgba(30, 41, 59, 0.85)`）＋ 薄いボーダー。
* **レイアウト:** 横並びの3カラム（スマホでは縦並び）。
* **各要素:**
    * **積みゲー総数:** Lucide `Gamepad2` アイコン ＋ 大きな数字。
    * **クリア済み:** Lucide `CheckCircle` アイコン ＋ 数字 ＋ **シアン色に発光する細いプログレスバー**（クリア率）。
    * **プレイ中:** Lucide `Trophy` アイコン ＋ 数字。
* **アイコン装飾:** ネオンのようにフワッと光るドロップシャドウを適用。

---

## 3. コントロールバー（フィルターとアクション）
HUDとゲーム一覧の間を繋ぐナビゲーションバー。

* **左側（フィルタータブ）:** * 「全て」「未着手」「プレイ中」「クリア済」のボタングループ。
    * 選択中のタブは、シアン色（または緑）に発光してアクティブ状態を示す。
* **右側（追加ボタン）:** * 「新規追加」ボタン。Lucide `Plus` アイコン ＋ テキスト。
    * ログインボタンと同じ「鮮やかな紫（`#7c3aed`）」を使用し、ホバーで浮き上がる最重要アクションとして配置。

---

## 4. ゲームグリッド（ライブラリ本体）
最も大きく変わる「Epic Games」風のポスター型コレクションエリア。

* **グリッドレイアウト:** 超コンパクト化。PCの大画面（xl）では1行に6枚、標準（lg）で4枚、スマホ（xs）で2枚並ぶように設定（`col-6 col-md-4 col-lg-3 col-xl-2`）。
* **カード形状:** パッケージやポスターを模した**縦長（アスペクト比 3:4 または 2:3）**。
* **デザイン（通常時）:**
    * 画像がカード全体を覆う（フルブリード）。
    * 画像が無い場合はダークブルー背景に Lucide `Image` アイコンを配置。
    * 下部に黒いシームレスなグラデーションを敷き、そこに「白文字のタイトル」と「ステータスを示す小さな色付きドット（緑や水色）」だけをシンプルに表示。
* **デザイン（ホバー・マウスオーバー時）:**
    * **カードの動き:** 全体が少しズームアップし、フワッと上に浮き上がる。
    * **発光:** カードの外枠がシアンまたは紫のネオンカラーで光る。
    * **情報の出現:** グラデーションが少し上に広がり、隠れていた情報（Lucide `Monitor` などのプラットフォームアイコン、評価の星）が出現。
    * **アクションの出現:** カードの右上に、小さくスタイリッシュな「編集（Lucide `Edit2`）」「削除（Lucide `Trash2`）」の丸いアイコンボタンがフワッと現れる。